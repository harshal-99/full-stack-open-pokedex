name: Hello World!

on:
  push:
    branches:
      - master
      - main

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: |
          echo "Hello World!"
      - name: Run Date
        run: |
          date
      - name: Run ls
        run: |
          ls
